[package]
name = "benchmark-consumer"
version = "0.2.0"
edition.workspace = true
publish = false

[features]
default = []
common = [
    "tracel-xtask/build",
    "tracel-xtask/check",
    "tracel-xtask/fix",
    "tracel-xtask/test",
]
all = ["tracel-xtask/all"]

[dependencies]
strum = { workspace = true }
tracel-xtask = { path = "../tracel-xtask", version = "=5.2.0", default-features = false }
