[package]
name = "xtask-tests"
version = "4.12.1"
edition = "2021"

[dependencies]
dotenvy = { workspace = true }
log = { workspace = true }
strum = { workspace = true }
tracel-xtask = { path = "../tracel-xtask" }

[dev-dependencies]
rstest = { workspace = true }
